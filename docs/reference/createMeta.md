# Create list of metadata from project environment.

Create list of metadata from project environment.

## Usage

``` r
createMeta(prj_name)
```

## Arguments

- prj_name:

  name of the project

## Value

list of metadata.

## Examples

``` r
if (FALSE) { # \dontrun{
createMeta(tempdir())
} # }
```
