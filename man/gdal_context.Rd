% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdalRun.R
\name{gdal_context}
\alias{gdal_context}
\title{Create a GDAL CLI context from linked binaries}
\usage{
gdal_context(
  binDir,
  env = character(),
  log_file = file.path(tempdir(), "gdal-run.ndjson"),
  help_cache_dir = file.path(tempdir(), "gdal-help-cache")
)
}
\arguments{
\item{binDir}{Character. Directory containing GDAL executables.}

\item{env}{Named character vector. Additional env vars (e.g., GDAL_DATA, PROJ_LIB).}

\item{log_file}{Character. NDJSON log file path (default: "gdal-run.ndjson" in tempdir()).}

\item{help_cache_dir}{Character. Directory for cached help texts (default: "gdal-help-cache" in tempdir()).}
}
\value{
A list (class "gdal_context") with resolved executables and env.
}
\description{
Resolves executable paths for GDAL utilities (e.g., gdalinfo, gdalwarp, ogr2ogr)
under a specific binDir and prepares a per-call environment (PATH + optional vars).
}
