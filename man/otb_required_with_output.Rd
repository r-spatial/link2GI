% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/otbIntro.R
\name{otb_required_with_output}
\alias{otb_required_with_output}
\title{Required parameter keys under link2GI output policy}
\usage{
otb_required_with_output(algo, gili = NULL, enforce_output = TRUE)
}
\arguments{
\item{algo}{Character scalar. OTB application name (e.g. \code{"BandMathX"}).}

\item{gili}{Optional list returned by \code{\link[link2GI:linkOTB]{link2GI::linkOTB}}.
If \code{NULL}, \code{link2GI::linkOTB()} is called internally.}

\item{enforce_output}{Logical. If \code{TRUE} (default), include the first matching
file output key found in the application's parameter set.}
}
\value{
Character vector of required keys (without leading \code{-}).
}
\description{
Extends \code{\link{otb_required}} by optionally enforcing the presence of at least
one file-based output parameter key in the returned set. This reflects the package
policy "always write outputs to disk" even if OTB marks output parameters as optional.
}
\details{
The function does \emph{not} guess output pixel types or create temporary file names.
It only returns keys.
}
\examples{
\dontrun{
otb <- link2GI::linkOTB()
if (isTRUE(otb$exist)) {
  otb_required_with_output("DimensionalityReduction", otb)
}
}
}
